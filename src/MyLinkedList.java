public class MyLinkedList {
    //addFirst: which adds a new element at the beginning of the linked list
    public <E> void addFirst(E element) {
    }

    //removeFirst: which removes and returns the first element from the linked list
    public <E> E removeFirst() {
        return null;
    }

    //getFirst: which returns the first element from the linked list without removing it
    public <E> E getFirst(){
        return null;
    }

    //isEmpty: which returns a boolean value indicating whether the linked list is empty or not
    public boolean isEmpty() {
        return false;
    }

    //size: which returns an integer representing the number of elements in the linked list
    public int size() {
        return 0;
    }

    //addLast: which adds a new element at the end of the linked list
    public <T> void addLast(T item) {
    }
}
